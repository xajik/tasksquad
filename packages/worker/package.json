{
  "name": "@tasksquad/worker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "build": "wrangler deploy --dry-run",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1004.0",
    "@aws-sdk/s3-request-presigner": "^3.1004.0",
    "aws4fetch": "^1.0.20",
    "cloudfire-auth": "^0.4.0",
    "itty-router": "^5.0.18",
    "ulidx": "^2.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.71.0"
  }
}
