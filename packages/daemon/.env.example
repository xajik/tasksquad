# Override the portal URL opened during `tsq login`.
# Set this for local development so the browser opens localhost instead of production.
# TSQ_DASHBOARD_URL=http://localhost:5173

# Amplitude analytics (optional)
# To enable, add to your config.toml:
# [analytics]
# enabled = true
# api_key = "your-amplitude-api-key"
